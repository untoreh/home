let os = pyimport("os"); os.getcwd().to(string)
print(let os = pyimport("os"); os.getcwd().to(string))
print(pyimport("os"); os.getcwd().to(string))
print(pyimport("os").getcwd().to(string))
print("wow")
display("wow")
echo "wow"
echo("wow")
echo pyimport("os").getcwd().to(string)
echo os.getcwd().to(string)
import nimpy
let os = pyimport("os")
os.getcwd().to(string)
echo os.getcwd().to(string)
echo (let os = pyimport("os")); os.getcwd().to(string)
echo (let os = pyimport("os"); os.getcwd().to(string));
let s = os.getcwd().to(string)
let s = pyimport("os").getcwd().to(string)
import nimpy
let os = pyImport("os")
echo "Current dir is: ", os.getcwd().to(string)
import nimpy
echo asd
echo "asd"
import nimpy
ls()
pwd()
import os
os.sleep(123)
proc nice=
  echo "start"
  sleep 10000
  echo "done"
import nimpy
proc invalid*(): string =
  result = "foo"
  "invalid"
let procVar = (proc(x: string))(p)
ls
ls()
ps
ps()
let procVar = (proc(x: string))(p)
proc p(x: int) = echo "int"
proc p(x: string) = echo "string"
let procVar = (proc(x: string))(p)
procvar("asD")
result = procvar("asD")
var result = procvar("asD")
var result: string = procvar("asD")
let procVar = p
let procVar = (p)
p(3)
return p(3)
echo p(3)
procVar("ASD")
procVar(1)
let procVar = (proc(x: string))(p)
let procVar(x: string) = p(x)
proc procVar(x: string) = p(x)
proc procVars(x: string) = p(x)
procvars("sad")
procvars("1"")
procvars("1")
procvars(1)
procvar(1)
var t1 = "asd"
t1
typeof(asd)
typeof(t1)
echo typeof(t1)
echo addr(t1)
echo repr(addr(t1))
t3: pointer = addr(t1)
t3 : pointer = addr(t1)
var t3: pointer = addr(t1)
t3
t3[]
echo cast[ptr string](t3)
echo cast[ptr string](t3)[]
echo repr(cast[ptr string](t3))
echo $cast[ptr string](t3)
echo $(cast[ptr string](t3))
unsafeAddr [1 ,2 ,3]
let wow = [1 ,2 ,3 ]
let a = [1, 2, 3]
unsafeAddr(a)
unsafeAddr a
echo (unsafeAddr a)
echo unsafeAddr(a)
echo repr(unsafeAddr(a))
echo repr(unsafeAddr a)
defaultFmtStr
dirname(os)
import logging
logging
import std/logging
var logger = newConsoleLogger(fmtStr="[$time] - $levelname: ")
logger.log(lvlInfo, "this is ame")
